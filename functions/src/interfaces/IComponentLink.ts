export interface IComponentLink {
  type: string; // indicates the type of the link ie, play store, apple store, web app, blog post
  url: string; // url for the link
  text: string; // text for the link
}
